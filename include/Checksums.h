#ifndef CHECKSUMS_H
#define CHECKSUMS_H

class Checksums
{
    public:

        Checksums();
        virtual ~Checksums();

        static void crc16() {;};
        static void crc32() {;};
        static void crc64() {;};

    protected:

    private:
};

#endif // CHECKSUMS_H
